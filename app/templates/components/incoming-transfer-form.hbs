<fieldset>
  <legend>{{title}}</legend>
  {{date-field value=model.date}}
  {{text-field title="Transfer Id" value=model.transferId}}
  {{money-field title="Value" value=model.value}}
  <div class="submit">
    <button type="submit">{{submitText}}</button>
  </div>
</fieldset>
