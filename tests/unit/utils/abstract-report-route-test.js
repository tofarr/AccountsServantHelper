import abstractReportRoute from 'kh-accounts/utils/abstract-report-route';
import { module, test } from 'qunit';

module('Unit | Utility | abstract-report-route', function(/*hooks*/) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = abstractReportRoute();
    assert.ok(result);
  });
});
