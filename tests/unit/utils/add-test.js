import add from 'kh-accounts/utils/add';
import { module, test } from 'qunit';

module('Unit | Utility | add', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = add();
    assert.ok(result);
  });
});
