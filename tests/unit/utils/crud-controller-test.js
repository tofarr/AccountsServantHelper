import crudController from 'kh-accounts/utils/crud-controller';
import { module, test } from 'qunit';

module('Unit | Utility | crud-controller', function(/*hooks*/) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = crudController();
    assert.ok(result);
  });
});
