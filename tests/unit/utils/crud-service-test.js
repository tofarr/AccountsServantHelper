import crudService from 'kh-accounts/utils/crud-service';
import { module, test } from 'qunit';

module('Unit | Utility | crud-service', function(/*hooks*/) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = crudService();
    assert.ok(result);
  });
});
