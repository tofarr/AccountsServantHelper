import money from 'kh-accounts/utils/money';
import { module, test } from 'qunit';

module('Unit | Utility | money', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = money();
    assert.ok(result);
  });
});
